name: Build Android App APK

on:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Create App Code and Build
        run: |
          mkdir -p app/src/main/java/com/global/chatroom
          mkdir -p app/src/main/res/values
          
          echo 'package com.global.chatroom; import android.app.Activity; import android.os.Bundle; import android.webkit.WebView; import android.webkit.WebSettings; public class MainActivity extends Activity { @Override protected void onCreate(Bundle savedInstanceState) { super.onCreate(savedInstanceState); WebView webView = new WebView(this); WebSettings ws = webView.getSettings(); ws.setJavaScriptEnabled(true); webView.loadData("<html><body style=\"background:#111;color:#fff;text-align:center;padding-top:50px;\"><h2>App Running Successfully!</h2></body></html>", "text/html", "UTF-8"); setContentView(webView); } }' > app/src/main/java/com/global/chatroom/MainActivity.java
          
          cp AndroidManifest.xml app/src/main/AndroidManifest.xml
          
          echo "include ':app'" > settings.gradle
          echo "buildscript { repositories { google(); mavenCentral() } dependencies { classpath 'com.android.tools.build:gradle:7.4.2' } }" > build.gradle
          
          echo "apply plugin: 'com.android.application'" > app/build.gradle
          echo "android { namespace 'com.global.chatroom'; compileSdkVersion 33; defaultConfig { applicationId 'com.global.chatroom'; minSdk 21; targetSdk 33; versionCode 1; versionName '1.0' } }" >> app/build.gradle
          
          gradle assembleDebug

      - name: Upload APK File
        uses: actions/upload-artifact@v4
        with:
          name: GlobalLiveChat-App-APK
          path: app/build/outputs/apk/debug/*.apk